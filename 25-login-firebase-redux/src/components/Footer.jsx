import styles from '../styles/footer.module.sass';

let Footer = () => {
    return (
        <footer className={styles.container}>
            <p>Coooder users, por: Anderecc</p>
        </footer>
    );
};

export default Footer;
